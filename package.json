{
  "name": "complain",
  "version": "1.6.0",
  "description": "Mark methods as deprecated and warn the user when they're called",
  "main": "index.js",
  "scripts": {
    "test": "nyc --include=index.js --reporter=lcov --reporter=text-summary mocha",
    "test-ci": "nyc --include=index.js --reporter=lcovonly --reporter=text mocha"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/mlrawlings/complain.git"
  },
  "keywords": [
    "deprecate",
    "complain"
  ],
  "author": "Michael Rawlings",
  "license": "MIT",
  "devDependencies": {
    "mocha": "^6.1.4",
    "nyc": "^14.1.1"
  },
  "dependencies": {
    "error-stack-parser": "^2.0.1"
  }
}
